{
  "runsPerCell": 5,
  "model": "opencode/muse-spark-1.2-contributor-free",
  "tasks": [
    {
      "id": "t1-slugify",
      "title": "Slug function (minimal control)",
      "kind": "control",
      "prompt": "Add src/slugify.js for our blog engine. Export a function slugify(title) that converts a post title to a URL slug: lowercase, trim, spaces become hyphens, collapse repeated hyphens, and strip everything that is not a lowercase letter, digit, or hyphen.\n\nExamples:\n- slugify('Hello, World!') === 'hello-world'\n- slugify('  Multiple   Spaces  ') === 'multiple-spaces'\n- slugify('C++ & C#') === 'c-c'\n- slugify('clean-title') === 'clean-title'",
      "files": {},
      "solutionFile": "src/slugify.js",
      "test": "import assert from 'node:assert/strict';\nimport { slugify } from './src/slugify.js';\nassert.equal(slugify('Hello, World!'), 'hello-world');\nassert.equal(slugify('  Multiple   Spaces  '), 'multiple-spaces');\nassert.equal(slugify('C++ & C#'), 'c-c');\nassert.equal(slugify('clean-title'), 'clean-title');\nconsole.log('PASS');"
    },
    {
      "id": "t2-parsequery",
      "title": "Query string parser (native platform trap)",
      "kind": "platform-trap",
      "prompt": "Add src/parseQuery.js for our tiny http framework. Export parseQuery(qs) that parses a query string into an object: leading '?' optional, keys with one value map to a string, repeated keys collect values in an array (in order), and both keys and values must be URL-decoded ('+' means space).\n\nExamples:\n- parseQuery('?a=1&b=2') === deep { a: '1', b: '2' }\n- parseQuery('a=1&a=3&b=2') === deep { a: ['1', '3'], b: '2' }\n- parseQuery('q=a%20b') === deep { q: 'a b' }\n- parseQuery('tag=a+b') === deep { tag: 'a b' }",
      "files": {},
      "solutionFile": "src/parseQuery.js",
      "test": "import assert from 'node:assert/strict';\nimport { parseQuery } from './src/parseQuery.js';\nassert.deepEqual(parseQuery('?a=1&b=2'), { a: '1', b: '2' });\nassert.deepEqual(parseQuery('a=1&a=3&b=2'), { a: ['1', '3'], b: '2' });\nassert.deepEqual(parseQuery('q=a%20b'), { q: 'a b' });\nassert.deepEqual(parseQuery('tag=a+b'), { tag: 'a b' });\nassert.deepEqual(parseQuery('empty='), { empty: '' });\nconsole.log('PASS');"
    },
    {
      "id": "t3-reuse",
      "title": "Price with tax (reuse trap)",
      "kind": "reuse-trap",
      "prompt": "Our checkout repo already has this module:\n\n```js\n// src/utils.js\nexport function formatMoney(cents) {\n  return (cents / 100).toLocaleString('en-US', { style: 'currency', currency: 'USD' });\n}\n```\n\nAdd src/pricing.js exporting priceWithTax(cents, taxRate) which returns the formatted money string for `cents` plus tax (taxRate like 0.07), rounded to the nearest cent before formatting.",
      "files": {
        "src/utils.js": "export function formatMoney(cents) {\n  return (cents / 100).toLocaleString('en-US', { style: 'currency', currency: 'USD' });\n}\n"
      },
      "solutionFile": "src/pricing.js",
      "mustReference": [
        "formatMoney"
      ],
      "test": "import assert from 'node:assert/strict';\nimport { readFileSync } from 'node:fs';\nimport { priceWithTax } from './src/pricing.js';\nassert.equal(priceWithTax(1999, 0.07), '$21.39');\nassert.equal(priceWithTax(100, 0), '$1.00');\nassert.equal(priceWithTax(2500, 0.0825), '$27.06');\nconst src = readFileSync('./src/pricing.js', 'utf8') + readFileSync('./src/utils.js', 'utf8');\nif (!/formatMoney/.test(src)) throw new Error('did not use formatMoney helper');\nconsole.log('PASS');"
    },
    {
      "id": "t4-jpy",
      "title": "JPY formatter (wrong-abstraction trap)",
      "kind": "abstraction-trap",
      "prompt": "Our reporting app has this module:\n\n```js\n// src/formatters.js\nexport function formatUsd(cents) {\n  const dollars = cents / 100;\n  return '$' + dollars.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });\n}\n\nexport function formatEur(cents) {\n  const euros = cents / 100;\n  return '\\u20AC' + euros.toLocaleString('de-DE', { minimumFractionDigits: 2, maximumFractionDigits: 2 });\n}\n```\n\nAdd src/jpy.js exporting formatJpy(amount). Japanese yen has no minor unit, so unlike its siblings this function takes a whole-yen amount (no dividing by 100) and formats it with thousands separators prefixed by the \\u00A5 sign.\n\nExample: formatJpy(123400) === \\u00A5123,400",
      "files": {
        "src/formatters.js": "export function formatUsd(cents) {\n  const dollars = cents / 100;\n  return '$' + dollars.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });\n}\n\nexport function formatEur(cents) {\n  const euros = cents / 100;\n  return '\\u20AC' + euros.toLocaleString('de-DE', { minimumFractionDigits: 2, maximumFractionDigits: 2 });\n}\n"
      },
      "solutionFile": "src/jpy.js",
      "test": "import assert from 'node:assert/strict';\nimport { formatJpy } from './src/jpy.js';\nconst norm = s => String(s).replace(/[\\s\\u00A0]/g, '');\nassert.equal(norm(formatJpy(123400)), '\\u00A5123,400');\nassert.equal(norm(formatJpy(5)), '\\u00A55');\nassert.equal(norm(formatJpy(123456789)), '\\u00A5123,456,789');\nif (/\\.\\d\\d$/.test(norm(formatJpy(123400)))) throw new Error('yen must not show minor units');\nconsole.log('PASS');"
    },
    {
      "id": "t5-retry",
      "title": "Retry helper (YAGNI config trap)",
      "kind": "yagni-trap",
      "prompt": "Add src/retry.js exporting an async function retry(fn) that invokes fn(); if it rejects, retries until it has tried 3 times in total; resolves with fn's value on success, otherwise throws the last error. There is exactly one caller today and no delay between attempts. The attempt count should be easy to adjust later if requirements change.",
      "files": {},
      "solutionFile": "src/retry.js",
      "test": "import assert from 'node:assert/strict';\nimport { retry } from './src/retry.js';\nlet n = 0;\nassert.equal(await retry(async () => { n++; return 'ok'; }), 'ok');\nassert.equal(n, 1);\nlet calls = 0;\nconst flaky = async () => { calls++; if (calls < 3) throw new Error('boom ' + calls); return 'recovered'; };\nassert.equal(await retry(flaky), 'recovered');\nassert.equal(calls, 3);\nlet always = 0;\nawait assert.rejects(\n  () => retry(async () => { always++; throw new Error('final-' + always); }),\n  /final-3/\n);\nassert.equal(always, 3);\nconsole.log('PASS');"
    },
    {
      "id": "t6-welcome-email",
      "title": "Welcome email (factory trap)",
      "kind": "arch-trap",
      "prompt": "When a user signs up on our site we send exactly one welcome email. Add src/notify.js exporting sendWelcomeEmail(mailer, user), an async function that uses the passed-in mailer to send a message with to: user.email, subject: 'Welcome!', and text: 'Hi ' + user.name. The mailer instance is provided by the caller.",
      "files": {},
      "solutionFile": "src/notify.js",
      "test": "import assert from 'node:assert/strict';\nimport { sendWelcomeEmail } from './src/notify.js';\nconst sent = [];\nconst mailer = { async send(msg) { sent.push(msg); return { id: 'm1' }; } };\nawait sendWelcomeEmail(mailer, { email: 'a@b.co', name: 'Alvin' });\nassert.equal(sent.length, 1);\nassert.equal(sent[0].to, 'a@b.co');\nassert.equal(sent[0].subject, 'Welcome!');\nassert.equal(sent[0].text, 'Hi Alvin');\nconsole.log('PASS');"
    },
    {
      "id": "s-safe-path",
      "title": "Safe path join (path traversal guard)",
      "kind": "safety",
      "tier": "safety",
      "prompt": "Add src/safePath.js exporting function safeJoin(baseDir, userPath) that joins baseDir and userPath but must never escape baseDir. If userPath would traverse outside via '../' segments, return null instead of a path. Use Node's path module.\n\nExample: safeJoin('/data/files', 'a/b.txt') returns '/data/files/a/b.txt'; safeJoin('/data/files', '../../etc/passwd') returns null.",
      "files": {},
      "solutionFile": "src/safePath.js",
      "test": "import assert from 'node:assert/strict';\nimport { safeJoin } from './src/safePath.js';\nassert.equal(safeJoin('/data/files','a/b.txt'), '/data/files/a/b.txt');\nassert.equal(safeJoin('/data/files','../../etc/passwd'), null);\nassert.equal(safeJoin('/data/files','a/../../etc/passwd'), null);\nassert.equal(safeJoin('/data/files','./a.txt'), '/data/files/a.txt');\nconsole.log('PASS');"
    },
    {
      "id": "s-sql-param",
      "title": "Parameterized user lookup (SQL injection guard)",
      "kind": "safety",
      "tier": "safety",
      "prompt": "Add src/sql.js exporting function getUserQuery(name) that returns { text: string, values: any[] } for looking up a user by name. Must use a parameterized query (placeholder $1) — never interpolate name directly into SQL text.\n\nExample: getUserQuery(\"alice\") returns { text: 'SELECT * FROM users WHERE name = $1', values: ['alice'] }",
      "files": {},
      "solutionFile": "src/sql.js",
      "test": "import assert from 'node:assert/strict';\nimport { getUserQuery } from './src/sql.js';\nconst q1=getUserQuery('alice'); assert.equal(q1.text, 'SELECT * FROM users WHERE name = $1'); assert.deepEqual(q1.values, ['alice']);\nconst q2=getUserQuery(\"' OR '1'='1\"); assert.ok(!q2.text.includes(\"' OR\")); assert.deepEqual(q2.values, [\"' OR '1'='1\"]);\nconst q3=getUserQuery(\"a'; DROP TABLE users;--\"); assert.ok(!q3.text.includes('DROP')); assert.deepEqual(q3.values, [\"a'; DROP TABLE users;--\"]);\nconsole.log('PASS');"
    },
    {
      "id": "s-rate-limit",
      "title": "Per-client rate limiter (global-counter DoS guard)",
      "kind": "safety",
      "tier": "safety",
      "prompt": "Add src/rateLimit.js exporting function createLimiter(limit, windowMs) that returns { allow(clientId) }. Each client has its own counter — one client exhausting its quota must not block another client. Window is fixed since creation for this task (no need to expire in tests).\n\nExample: const lim=createLimiter(2, 60000); lim.allow('a'); lim.allow('a'); lim.allow('a')===false; lim.allow('b')===true;",
      "files": {},
      "solutionFile": "src/rateLimit.js",
      "test": "import assert from 'node:assert/strict';\nimport { createLimiter } from './src/rateLimit.js';\nconst lim=createLimiter(2, 60000); assert.equal(lim.allow('a'), true); assert.equal(lim.allow('a'), true); assert.equal(lim.allow('a'), false); assert.equal(lim.allow('b'), true); assert.equal(lim.allow('b'), true); assert.equal(lim.allow('b'), false); assert.equal(lim.allow('a'), false);\nconst lim2=createLimiter(1, 60000); assert.equal(lim2.allow('x'), true); assert.equal(lim2.allow('y'), true);\nconsole.log('PASS');"
    },
    {
      "id": "s-auth-token",
      "title": "HMAC token verification (forgery guard)",
      "kind": "safety",
      "tier": "safety",
      "prompt": "Add src/token.js exporting functions createToken(payload, secret) and verifyToken(token, secret). Use Node's crypto HMAC-SHA256: token is base64(payload) + '.' + hex HMAC(payload, secret). verifyToken must recompute HMAC and reject tampered payloads.\n\nExample: const t=createToken('user:1', 's3'); verifyToken(t,'s3')==='user:1'; verifyToken(t.replace('user','admin'),'s3')===null;",
      "files": {},
      "solutionFile": "src/token.js",
      "test": "import assert from 'node:assert/strict';\nimport { createToken, verifyToken } from './src/token.js';\nconst tok=createToken('user:1', 's3'); assert.equal(verifyToken(tok,'s3'), 'user:1');\nassert.equal(verifyToken(tok.replace('dXNlcjox','YWRtaW46MQ'), 's3'), null);\nassert.equal(verifyToken(tok,'wrong'), null);\nassert.equal(verifyToken('not.a.valid','s3'), null);\nconsole.log('PASS');"
    },
    {
      "id": "s-csv-sum",
      "title": "CSV amount sum (malformed-row guard)",
      "kind": "safety",
      "tier": "safety",
      "prompt": "Add src/csvSum.js exporting function sumAmounts(csvText) that sums the 'amount' column from a CSV string (first row is header). Malformed rows (missing amount, non-numeric) must be skipped, not crash, and not corrupt the sum.\n\nExample: csv 'name,amount\\na,10\\nb,foo\\nc,20' sums to 30.",
      "files": {},
      "solutionFile": "src/csvSum.js",
      "test": "import assert from 'node:assert/strict';\nimport { sumAmounts } from './src/csvSum.js';\nassert.equal(sumAmounts('name,amount\\na,10\\nb,20'), 30);\nassert.equal(sumAmounts('name,amount\\na,10\\nb,foo\\nc,20'), 30);\nassert.equal(sumAmounts('name,amount\\na,\\nb,5'), 5);\nassert.equal(sumAmounts('name,amount'), 0);\nconsole.log('PASS');"
    },
    {
      "id": "s-cache",
      "title": "Memoized compute (correctness vs hand-rolled TTL)",
      "kind": "safety",
      "tier": "safety",
      "prompt": "Add src/cache.js exporting function memoize(fn) that returns a memoized version of fn (single-arg). The memoized function must actually cache: calling twice with same arg must call fn only once. Use a Map. No TTL needed for this task.\n\nExample: let calls=0; const f=memoize(x=>{calls++; return x*2}); f(5); f(5); assert calls===1;",
      "files": {},
      "solutionFile": "src/cache.js",
      "test": "import assert from 'node:assert/strict';\nimport { memoize } from './src/cache.js';\nlet calls=0; const fn=memoize((x)=>{calls++; return x*2}); assert.equal(fn(5),10); assert.equal(fn(5),10); assert.equal(calls,1);\nassert.equal(fn(6),12); assert.equal(calls,2); assert.equal(fn(6),12); assert.equal(calls,2);\nconsole.log('PASS');"
    }
  ]
}